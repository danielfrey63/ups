CREATE TABLE ANMELDEDATEN(ID BIGINT GENERATED BY DEFAULT AS IDENTITY(START WITH 1)  NOT NULL PRIMARY KEY,DOZENTUSERNAME VARCHAR(255),PRUEFUNGSMODETEXT VARCHAR(255),FACHRICHTUNG VARCHAR(255),STUDENTENNUMMER VARCHAR(255),VORNAME VARCHAR(255),REPETENT BOOLEAN,PRUEFUNGSRAUM VARCHAR(255),NACHNAME VARCHAR(255),STUDIENGANG VARCHAR(255),EMAIL VARCHAR(255),LKNUMMER VARCHAR(255),LKFORM INTEGER,LKFORMTEXT VARCHAR(255),LKEINHEITTITEL VARCHAR(255),LKEINHEITTYP VARCHAR(255),LKEINHEITNUMMERZUSATZ VARCHAR(255),LKEINHEITTYPTEXT VARCHAR(255),SESKEZ VARCHAR(255),PRUEFUNGSDATUM TIMESTAMP,PRUEFUNGSDATUMVON TIMESTAMP,PRUEFUNGSDATUMBIS TIMESTAMP)
CREATE TABLE OIS2LDAP(LDAP VARCHAR(255) NOT NULL PRIMARY KEY,OISUSERNAME VARCHAR(255),OISPASSWORD VARCHAR(255))
CREATE TABLE PRUEFUNGSLISTE(ID BIGINT GENERATED BY DEFAULT AS IDENTITY(START WITH 1)  NOT NULL PRIMARY KEY,PRUEFUNGSSESSION VARCHAR(255),PRUEFUNG VARCHAR(255),USERNAME VARCHAR(255),STUDENTENNUMMER VARCHAR(255),PRUEFUNGSLISTE LONGVARBINARY,PDF LONGVARBINARY,PROPERTIES LONGVARBINARY)
CREATE TABLE PRUEFUNGSSESSION(SESKEZ VARCHAR(255) NOT NULL PRIMARY KEY,SESSIONSNAME VARCHAR(255),SESSIONSENDE TIMESTAMP,PLANUNGFREIGABE TIMESTAMP,STORAGEDATE TIMESTAMP)
CREATE USER SA PASSWORD "" ADMIN
