<?xml version="1.0" encoding="UTF-8"?>
<project>
    <parent>
        <groupId>ch.xmatrix</groupId>
        <artifactId>ch.xmatrix.build</artifactId>
        <version>1.0</version>
        <relativePath>../BuildCommon</relativePath>
    </parent>
    <modelVersion>4.0.0</modelVersion>
    <groupId>ch.xmatrix</groupId>
    <artifactId>ch.xmatrix.ups.data</artifactId>
    <name>All Data</name>
    <packaging>pom</packaging>
    <version>1.0</version>
    <description>Data and Configuration</description>
    <modules>
        <module>DataConstraints</module>
        <module>DataCourses</module>
        <module>DataExams</module>
        <module>DataSessions</module>
        <module>DataTaxa</module>
    </modules>
    <build>
        <plugins>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-jar-plugin</artifactId>
                <configuration>
                    <archive>
                        <manifest>
                            <addDefaultImplementationEntries>true</addDefaultImplementationEntries>
                        </manifest>
                        <manifestEntries>
                            <Main-Class>data.Main</Main-Class>
                            <Subversion-Build>${project.version}-${buildNumber}</Subversion-Build>
                        </manifestEntries>
                    </archive>
                </configuration>
            </plugin>
        </plugins>
    </build>
</project>